# E2E Test Issues

## 2026-03-16 — Run 1

1. **P1** `[BROKEN]` `/test` not available as a Skill — `Skill("test")` and `Skill("vibe-closer")` both return "Unknown skill". Test must be followed manually. **Fix:** Register `/test` as a discoverable command or fix plugin skill discovery.
